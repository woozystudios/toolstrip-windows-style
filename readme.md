# ToolStrip Windows Style

Example and test of recreating a WindowsForms ToolStrip for an Electron app.

## Deployment

Run `npm install` in order to install the required dependencies and then run `npm start` to run Electron App.
